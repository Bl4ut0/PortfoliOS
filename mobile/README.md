# PortfoliOS Mobile Shell (Android/iOS UX)

This directory is dedicated to the mobile device simulation interface. Currently, it houses the core view orchestrator (`shell.js`).

---

## Future Goals
- Expand mobile app experiences (e.g. customized widgets, standalone games, or responsive cards).
- Support full touch drag/gestures.
- Expand app-specific mobile views under a dedicated subfolder to keep files separated.
